{
  "name": "asms",
  "version": "0.3.1",
  "private": true,
  "description": "ASMS - Ark Server Management Suite. A sleek, self-hosted manager for ARK: Survival Ascended dedicated servers.",
  "workspaces": [
    "apps/server",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace @asms/server run dev",
    "dev:web": "npm --workspace @asms/web run dev",
    "build": "npm --workspace @asms/web run build && npm --workspace @asms/server run build",
    "start": "node apps/server/dist/index.js",
    "typecheck": "npm --workspace @asms/server run typecheck && npm --workspace @asms/web run typecheck",
    "test": "npm --workspace @asms/server run test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^15.15.0",
    "typescript-eslint": "^8.69.0"
  },
  "engines": {
    "node": ">=20"
  }
}
